Simple experiment about backbone, a javascript framework (It uses flask server side).

In order to try it you should initialize the virtualenv and
launch the server

 $ virtualenv env
 $ source env/bin/activate
 $ pip install -r dependencies.txt
 $ python server.py

and then you can visit localhost:5000.
